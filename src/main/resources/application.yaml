spring:
  datasource:
    url: jdbc:mysql://localhost:3306/BTS
    username: root
    password: dlalstjr1!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace

jwt:
  secret: secret
  access_expiration: 3600
  refresh_expiration: 86400


server:
  port: 8080
